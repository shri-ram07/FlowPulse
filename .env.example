# Copy to .env and fill in as needed. All variables are OPTIONAL — the demo
# runs end-to-end without any of them (agents fall back to deterministic
# reasoning, JWT uses a dev secret, FCM runs dry-run, Cloud Trace no-ops).

# ---- JWT signing secret for staff auth -------------------------------
# Generate with: py -3 -c "import secrets; print(secrets.token_urlsafe(48))"
FLOWPULSE_JWT_SECRET=dev-secret-change-me

# ---- Google AI / Gemini via ADK --------------------------------------
# Free key at https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# Gemini model override. Default (in backend/agents/config.py) is
# `gemini-3-flash-preview` — Google's newest Flash generation. If you hit
# AI-Studio free-tier 429 RESOURCE_EXHAUSTED during heavy traffic, flip to
# `gemini-2.5-flash` (GA, higher quota) without a rebuild:
#   gcloud run services update flowpulse-backend --region=asia-south1 \
#     --update-env-vars=FLOWPULSE_GEMINI_MODEL=gemini-2.5-flash
FLOWPULSE_GEMINI_MODEL=

# ---- FCM HTTP v1 (service-account JSON) ------------------------------
# Absolute path to a service-account key file. The legacy FCM Server Key
# has been deprecated by Google — use this instead.
# Firebase Console → Project settings → Service accounts → Generate new
# private key. Save the JSON OUTSIDE the repo.
GOOGLE_APPLICATION_CREDENTIALS=

# ---- Cloud Trace target project --------------------------------------
# Your GCP project ID. Enables OpenTelemetry spans export, custom
# Cloud Monitoring metrics, and BigQuery event streaming.
GOOGLE_CLOUD_PROJECT=

# ---- Vertex AI dual-path ---------------------------------------------
# When set to "1" or "true" in a deployed Cloud Run service, ADK routes
# every Gemini call through Vertex AI (uses Application Default
# Credentials from the runtime service account) instead of the AI Studio
# API key. Same Python code runs in dev (AI Studio) and prod (Vertex).
GOOGLE_GENAI_USE_VERTEXAI=
GOOGLE_CLOUD_LOCATION=asia-south1

# ---- BigQuery sink overrides (optional) ------------------------------
# Defaults: FLOWPULSE_BQ_DATASET=flowpulse_events  FLOWPULSE_BQ_TABLE=ticks
FLOWPULSE_BQ_DATASET=
FLOWPULSE_BQ_TABLE=

# Disable any one observability integration during local dev:
# FLOWPULSE_DISABLE_METRICS=1
# FLOWPULSE_DISABLE_BIGQUERY=1

# ---- Log verbosity ---------------------------------------------------
FLOWPULSE_LOG_LEVEL=INFO
